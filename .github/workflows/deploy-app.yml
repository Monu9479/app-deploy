name: deploy-app
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "Deploying app…"
